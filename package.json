{
  "name": "lite-agent",
  "version": "0.0.0",
  "private": true,
  "description": "Pluggable, lightweight agent-core SDK — monorepo root.",
  "type": "module",
  "license": "ISC",
  "packageManager": "pnpm@10.12.4",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "dev": "pnpm --filter @lite-agent/example-cli dev",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "pnpm build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.10",
    "typescript": "^6.0.2"
  }
}
